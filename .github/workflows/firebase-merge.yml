# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase on merge
on:
  push:
    branches:
      - main
    pull_request:
      branches:
        -main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # - run: [install firebase using npm]
      # - run: [write the contents of secrets.FIREBASE_SERVICE_ACCOUNT_BAILEYMENDEL_FIREBASE into a file ]
      - run: firebase deploy
        # env: 
        #   GOOGLE_APPLICATION_CREDENTIALS: "[name of file from above]"
