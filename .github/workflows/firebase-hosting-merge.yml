# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      # to do: install firebase + set up auth
      # install firebase with NPM install
      # 

      - run: firebase deploy

      # env:
      # GOOGLE_APPLICATION_CREDENTIALS - variable name we need to add
      # contains name of file, contents of file should be contents of GitHub Secret
      # write contents of secret to a file
      # pass name of that file via google application 